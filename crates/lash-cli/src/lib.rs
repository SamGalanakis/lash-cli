//! Application support for the `lash` binary.
//!
//! This crate is private to the lash-cli product workspace. Its configuration
//! and filesystem layout are host policy, not Lash runtime APIs.

pub mod config;
pub mod execution_settings;
pub mod paths;
